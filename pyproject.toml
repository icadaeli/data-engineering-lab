[project]
name = "data-engineering-lab"
version = "0.1.0"
description = "Hands-on practice with Python, SQL, databases, and data engineering tools."
readme = "README.md"
authors = [
    { name = "Ica Daeli" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
